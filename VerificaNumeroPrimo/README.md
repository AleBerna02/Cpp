# Verifica numero primo

Scrivere un programma che chiede all'utente un numero intero positivo e verifica se si tratta di un numero primo. Il programma deve stampare "Primo" se lo è, altrimenti "Non primo".

Il programma deve essere scritto in un file chiamato `isprime.cpp`.