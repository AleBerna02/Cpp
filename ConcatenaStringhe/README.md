# Concatenazione di due stringhe

Scrivere un programma che chiede e legge due stringhe da input, e concatena la seconda alla fine della prima senza usare funzioni di libreria (come strcat). Il programma deve poi stampare la stringa concatenata risultante.

Il programma deve essere scritto in un file chiamato `stringconcat.cpp`.