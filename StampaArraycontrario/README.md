# Stampa array al contrario

Scrivere un programma che definisce una procedura (funzione `void`) per stampare gli elementi di un array di interi in ordine inverso rispetto a come sono stati inseriti.

Il programma deve essere scritto in un file chiamato `reversearray.cpp`.