# Concatenazione di due array

Scrivere un programma che definisce una funzione per concatenare due array di interi in un terzo array. Il main deve leggere le dimensioni e i valori dei primi due array, richiamare la funzione e stampare l'array risultante.

Il programma deve essere scritto in un file chiamato `concatarray.cpp`.