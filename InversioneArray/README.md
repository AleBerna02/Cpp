# Inversione di un array sul posto

Scrivere un programma che definisce una funzione per invertire l'ordine degli elementi di un array direttamente al suo interno (modificando l'array originale). Il main deve stampare l'array dopo la modifica.

Il programma deve essere scritto in un file chiamato `reverseinplace.cpp`.