# Menu con quattro alternative

Scrivere un programma che propone all'utente un menu con quattro alternative, ne legge la scelta e seleziona l'alternativa corrispondente. Il ciclo continua finché l'utente non sceglie di uscire.

Il programma deve essere scritto in un file chiamato `menu.cpp`.