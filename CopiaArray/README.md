# Copia di un array

Scrivere un programma che definisce una funzione per copiare il contenuto di un array di interi in un secondo array. Il main deve stampare il secondo array per verificare l'avvenuta copia.

Il programma deve essere scritto in un file chiamato `copyarray.cpp`.