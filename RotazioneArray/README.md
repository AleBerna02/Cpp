# Rotazione di un array

Scrivere un programma che definisce una funzione per ruotare verso sinistra di una posizione tutti gli elementi di un array (il primo elemento diventa l'ultimo). Il main legge i dati, esegue la rotazione e stampa l'array modificato.

Il programma deve essere scritto in un file chiamato `rotatearray.cpp`.