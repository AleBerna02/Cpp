# Somma degli elementi di un array

Scrivere un programma che utilizza una funzione per calcolare la somma di tutti gli elementi presenti in un array di interi. Il main deve occuparsi della lettura dell'array e della stampa della somma.

Il programma deve essere scritto in un file chiamato `sumarray.cpp`.