# Array ordinato

Scrivere un programma che definisce una funzione booleana per verificare se un array di interi è ordinato in senso crescente (o non decrescente). Il main legge i dati e stampa il responso.

Il programma deve essere scritto in un file chiamato `issorted.cpp`.