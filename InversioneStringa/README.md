# Inversione di una stringa

Scrivere un programma che chiede e legge una stringa da input e ne inverte l'ordine dei caratteri sul posto (modificando l'array originale), senza usare funzioni di libreria come strrev. Infine, stampa la stringa invertita.

Il programma deve essere scritto in un file chiamato `stringreverse.cpp`.