# Scambio circolare verso sinistra

Scrivere un programma che scambia in maniera circolare "verso sinistra" i valori di tre variabili di tipo float, lette da input, e stampa i valori prima e dopo lo scambio.

Il programma deve essere scritto in un file chiamato `rotleft.cpp`.