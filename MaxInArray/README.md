# Massimo in un array

Scrivere un programma che definisce una funzione per trovare il valore massimo in un array di interi. Il programma principale deve leggere la dimensione dell'array, leggere gli elementi da input, chiamare la funzione e stampare il risultato.

Il programma deve essere scritto in un file chiamato `maxarray.cpp`.