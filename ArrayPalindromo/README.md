# Array palindromo

Scrivere un programma che definisce una funzione booleana per verificare se un array di interi è palindromo (cioè se letto da sinistra verso destra o da destra verso sinistra risulta identico).

Il programma deve essere scritto in un file chiamato `ispalindrome.cpp`.