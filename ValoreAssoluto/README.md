# Valore assoluto

Scrivere un programma che legge un numero intero e ne stampa il valore assoluto (ovvero il numero senza segno, ad esempio se leggo -7 devo stampare 7).

Il programma deve essere scritto in un file chiamato `absval.cpp`.